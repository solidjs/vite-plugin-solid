export default function LazyTmp() {
  return (
    <div class="lazy-tmp-page">
      <h2>LazyLinkTmp</h2>
      <p class="tmp-probe">
        The fallback mounted lazyLinkTmp.css (red) while this module loaded; once mounted, the link
        is gone and this text must not be red.
      </p>
    </div>
  );
}
