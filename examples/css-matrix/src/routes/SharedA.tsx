import { sharedLabel } from '../shared/theme';

export default function SharedA() {
  return (
    <div class="shared-a-page">
      <h2>SharedA</h2>
      <p class="shared-probe">{sharedLabel}</p>
    </div>
  );
}
