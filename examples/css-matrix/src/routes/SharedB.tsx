import { sharedLabel } from '../shared/theme';

export default function SharedB() {
  return (
    <div class="shared-b-page">
      <h2>SharedB</h2>
      <p class="shared-probe">{sharedLabel}</p>
    </div>
  );
}
